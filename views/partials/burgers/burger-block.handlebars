<li>
	{{burger_name}}

	<button class="change-sleep" data-id="{{id}}" data-devour="{{eaten}}">
		{{#if devoured}}NEEDS TO BE EATEN{{else}}Eaten!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
